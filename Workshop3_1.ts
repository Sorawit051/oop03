//function caiculateArea(width: number, height: number): number {
//    return width * height;
//}

//let area:number = caiculateArea(5, 10);

const caiculateArea = (width: number, height: number): number => {
    return width * height;
}

console.log(`Area: ${caiculateArea(5, 10)}`);